This example shows a minimal example of how to structure a CMake project. The folder structure is quite common in larger projects.

How to compile with Make:
```bash
mkdir build
cd build/

cmake ..

make
```
